{
  "name": "tivuq-license-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch src/local.js",
    "start": "node src/local.js",
    "db:migrate": "node scripts/migrate.js",
    "keys:generate": "node scripts/generate_keys.js",
    "production:check": "node scripts/production_check.js",
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
